server:
  port: 8081

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: Qwerty1,0*

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8671/eureka

# Custom configs
ratings:
  service:
    url: http://RATING-SERVICE/ratings/users/

hotel:
  service:
    url: http://HOTEL-SERVICE/hotels/

logging:
  level:
    root: info
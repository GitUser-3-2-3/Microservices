server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/microservices
    username: postgres
    password: Qwerty1,0*

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true